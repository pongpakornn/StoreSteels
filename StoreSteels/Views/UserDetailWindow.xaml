<Window x:Class="StoreSteels.Views.UserDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Management" Height="560" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="AnimatedButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource MainPurple}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" CornerRadius="12" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="buttonScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="buttonScale" Storyboard.TargetProperty="ScaleX" To="1.03" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="buttonScale" Storyboard.TargetProperty="ScaleY" To="1.03" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="buttonScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="buttonScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CyberInput" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="#E0E0E0" BorderThickness="1.5">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>
                                <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Foreground="#AAA" IsHitTestVisible="False" VerticalAlignment="Center" Margin="15,0" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CyberPassword" TargetType="PasswordBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="#E0E0E0" BorderThickness="1.5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CyberCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="mainBorder" 
                            CornerRadius="10" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="1.5"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="35"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}" 
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" 
                                          HorizontalAlignment="Center"/>

                                <Path Grid.Column="1" 
                              Data="M 0 0 L 4 4 L 8 0" 
                              Stroke="#AAA" 
                              StrokeThickness="2" 
                              VerticalAlignment="Center" 
                              HorizontalAlignment="Center"/>
                            </Grid>

                            <ToggleButton Background="Transparent" 
                                  BorderThickness="0" 
                                  Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True">
                                <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}" Margin="0,5,0,5">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="mainBorder" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                                <Setter TargetName="mainBorder" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="mainBorder" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="25" BorderBrush="#EEE" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.1" ShadowDepth="3"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="txtTitle" Text="USER REGISTRATION" FontSize="22" FontWeight="Black" Foreground="{StaticResource MainPurple}"/>
                <Rectangle Height="3" Fill="{StaticResource AccentPurple}" Width="40" HorizontalAlignment="Left" Margin="2,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="EMPLOYEE ID (USER ID)" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,0,0,5"/>
                <TextBox x:Name="txtEmpID" FontWeight="Bold" Style="{StaticResource CyberInput}" Tag="Ex: 66001..." Margin="0,0,0,15"/>

                <TextBlock Text="PASSWORD" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Style="{StaticResource CyberPassword}" Margin="0,0,0,15"/>

                <TextBlock Text="FULL NAME" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,0,0,5"/>
                <TextBox x:Name="txtFullName" FontWeight="Bold" Style="{StaticResource CyberInput}" Tag="Enter full name..." Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="POSITION" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,0,0,5"/>
                        <TextBox x:Name="txtPosition" FontWeight="Bold" Style="{StaticResource CyberInput}" Tag="Role title..."/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="DEPARTMENT" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,0,0,5"/>
                        <ComboBox x:Name="cbDept" FontWeight="Bold" Style="{StaticResource CyberCombo}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="ROLE LEVEL (PERMISSION)" FontWeight="ExtraBold" Foreground="{StaticResource AccentPurple}" FontSize="11" Margin="5,15,0,5"/>
                <ComboBox x:Name="cbRole" FontWeight="Bold" Style="{StaticResource CyberCombo}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
                <Button Content="CANCEL" Click="Cancel_Click" Background="#F5F5F5" Foreground="#666" Width="100" Height="45" Margin="0,0,10,0" Style="{StaticResource AnimatedButton}"/>
                <Button x:Name="btnSave" Content="SAVE USER" Click="Save_Click" Background="#4CAF50" Width="140" Height="45" Style="{StaticResource AnimatedButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>