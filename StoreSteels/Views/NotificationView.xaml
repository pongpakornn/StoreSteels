<Window x:Class="StoreSteels.Views.NotificationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Width="350" Height="100" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True">

    <Grid Margin="10">
        <Border x:Name="LayoutBorder" Background="#212121" CornerRadius="6">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="Black" Opacity="0.4"/>
            </Border.Effect>

            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <Grid Width="35" Height="35" Margin="0,0,12,0">
                        <Ellipse x:Name="IconCircle" Fill="#3498DB"/>
                        <TextBlock x:Name="txtIcon" Text="✓" Foreground="White" FontSize="18" 
                                   FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="txtTitle" Text="Success" Foreground="White" 
                                   FontSize="14" FontWeight="Bold"/>
                        <TextBlock x:Name="txtMessage" Text="Message here..." Foreground="#AAAAAA" 
                                   FontSize="11" TextWrapping="Wrap" MaxWidth="240"/>
                    </StackPanel>
                </StackPanel>

                <ProgressBar x:Name="ProgBar" Height="4" VerticalAlignment="Bottom" 
                             Background="Transparent" BorderThickness="0" 
                             Foreground="#3498DB" Minimum="0" Maximum="100" Value="100"/>
            </Grid>
        </Border>
    </Grid>
</Window>