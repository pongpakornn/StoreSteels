<Window x:Class="StoreSteels.Views.PrintProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Printing" Width="380" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Topmost="True">

    <Border x:Name="MainBorder" Background="#212121" CornerRadius="20" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Black" Opacity="0.6"/>
        </Border.Effect>

        <Border.RenderTransform>
            <ScaleTransform x:Name="WindowScale" CenterX="190" CenterY="100" ScaleX="0.8" ScaleY="0.8"/>
        </Border.RenderTransform>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <Ellipse Width="60" Height="60" Fill="#6750A4" VerticalAlignment="Top"/>
                <TextBlock Text="&#128424;" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-8,0,0"/>
            </Grid>

            <StackPanel Grid.Row="1" Margin="0,20,0,15">
                <TextBlock x:Name="lblTitle" Text="กำลังพิมพ์ Packing Card" Foreground="White" FontSize="18"
                           FontWeight="Black" HorizontalAlignment="Center"/>
                <TextBlock x:Name="lblStatus" Text="กำลังพิมพ์ 0 / 0" Foreground="#AAAAAA" FontSize="13"
                           HorizontalAlignment="Center" TextAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <ProgressBar x:Name="progPrint" Height="10" Minimum="0" Maximum="100" Value="0"
                             Background="#3A3A3A" Foreground="#6750A4" BorderThickness="0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
