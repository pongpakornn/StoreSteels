<Window x:Class="StoreSteels.Views.CustomWarningView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Warning" Width="380" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Topmost="True">

    <Window.Resources>
        <Style x:Key="WarningButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="#212121" CornerRadius="20" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Black" Opacity="0.6"/>
        </Border.Effect>

        <!-- แก้ไขตรงนี้ -->
        <Border.RenderTransform>
            <ScaleTransform x:Name="WindowScale" ScaleX="0.8" ScaleY="0.8"/>
        </Border.RenderTransform>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <Ellipse Width="65" Height="65" Fill="#E74C3C" VerticalAlignment="Top"/>
                <TextBlock x:Name="txtIcon" Text="!" Foreground="White" FontSize="40" FontWeight="Black" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-5,0,0">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="IconTranslate" X="0" Y="0"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Grid>

            <StackPanel Grid.Row="1" Margin="0,20,0,15">
                <TextBlock x:Name="lblTitle" Text="WARNING" Foreground="#E74C3C" FontSize="22" 
                           FontWeight="Black" HorizontalAlignment="Center"/>
                <TextBlock x:Name="lblMessage" Text="Message content..." Foreground="#DDDDDD" 
                           FontSize="14" HorizontalAlignment="Center" TextAlignment="Center" 
                           TextWrapping="Wrap" Margin="0,10,0,0"/>
            </StackPanel>

            <Grid Grid.Row="2">
                <Button Content="OK" Style="{StaticResource WarningButton}" 
                        Background="#E74C3C" Click="OK_Click" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Border>
</Window>